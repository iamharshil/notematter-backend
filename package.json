{
	"name": "notematter-backend",
	"module": "src/server.ts",
	"type": "module",
	"private": true,
	"author": "iamharshil",
	"scripts": {
		"build": "bun build src/server.ts --outdir=dist",
		"start": "bun run src/server.ts",
		"dev": "bun run --watch src/server.ts",
		"biome": "biome check"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.5",
		"@types/bun": "latest",
		"@types/dotenv-safe": "^9.1.0",
		"@types/express": "^5.0.5"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"dotenv-safe": "^9.1.0",
		"express": "^5.1.0",
		"pino": "^10.1.0",
		"pino-http": "^11.0.0",
		"pino-pretty": "^13.1.2"
	}
}
